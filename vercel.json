{
  "version": 2,
  "buildCommand": "npm run build:css",
  "builds": [
    {
      "src": "api/vercel.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**", "prisma/**", "public/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/css/(.*)",
      "dest": "/api/vercel.ts"
    },
    {
      "src": "/img/(.*)",
      "dest": "/api/vercel.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/api/vercel.ts"
    }
  ]
}
